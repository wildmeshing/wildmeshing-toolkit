[
  {
    "doc": "",
    "pointer": "/",
    "type": "object",
    "required": [
      "application",
      "input"
    ],
    "optional": [
      "output",
      "input_dir",
      "offset_selection",
      "offset_output_tags",
      "protected_tags",
      "offset_in",
      "offset_out",
      "target_distance",
      "target_distance_rel",
      "throw_on_nonconvergence",
      "envelope_size",
      "envelope_size_rel",
      "offset_field",
      "offset_dhat_factor",
      "front_conv_rel",
      "front_conv_criterion",
      "offset_residual_samples",
      "sorted_marching",
      "check_manifoldness",
      "save_vtu",
      "phi_grid_resolution",
      "DEBUG_output",
      "DEBUG_output_per_pass",
      "log_file",
      "report",
      "preallocation_factor",
      "num_threads",
      "num_smoothing_passes",
      "interleaved_smoothing",
      "interleaved_smoothing_passes",
      "max_iterations",
      "ab_smooth_max_passes",
      "ab_smooth_tol",
      "vertex_grad_tol_rel",
      "offset_envelope_rel",
      "length_rel",
      "length",
      "stop_energy",
      "min_edge_length",
      "min_edge_length_rel",
      "min_sizing_scalar",
      "max_sizing_scalar",
      "sizing_gradation",
      "split_high_valence_threshold",
      "coarsen_pass",
      "coarsen_unbounded",
      "coarsen_max_rounds",
      "coarsen_global_smoothing_passes",
      "coarsen_smooth_ring",
      "coarsen_local_smoothing_passes",
      "stuck_refine_stall_eps",
      "stuck_refine_cooldown",
      "stuck_refine_num_worst",
      "stuck_refine_rings",
      "stuck_refine_factor",
      "stuck_refine_min_scalar",
      "stuck_refine_gradation",
      "stuck_refine_force_split",
      "front_normal_projection",
      "front_alignment_energy",
      "sizing_collapse_min",
      "max_rounds",
      "pre_optimize_sizing_from_edges",
      "pre_optimize_input",
      "w_amips",
      "smoothing_mode",
      "project_line_search_steps",
      "project_line_search_nested_steps",
      "smooth_quality_veto",
      "perform_sanity_checks",
      "deform_others"
    ]
  },
  {
    "pointer": "/application",
    "type": "string",
    "options": [
      "topological_offset"
    ],
    "doc": "Application name must be topological_offset."
  },
  {
    "pointer": "/input",
    "type": "string",
    "doc": "Tetrahedral input mesh."
  },
  {
    "pointer": "/output",
    "type": "string",
    "default": "out",
    "doc": "Output file name (without extension)."
  },
  {
    "pointer": "/input_dir",
    "type": "string",
    "default": "",
    "doc": "Directory where the input files are located. This is injected by the application and should not be set by the user."
  },
  {
    "pointer": "/offset_selection",
    "type": "string",
    "default": "!_",
    "doc": "Boolean expression for input simplicial complex to offset. If a single tag (ie 'tag_0') is given, single body mode is used."
  },
  {
    "pointer": "/offset_output_tags",
    "type": "list",
    "default": [
      "newtag"
    ],
    "doc": "Tags to add to elements in the resulting offset region."
  },
  {
    "pointer": "/offset_output_tags/*",
    "type": "string",
    "doc": "A tag."
  },
  {
    "pointer": "/protected_tags",
    "type": "list",
    "default": [],
    "doc": "Set of tags that will not be overwritten by the offset."
  },
  {
    "pointer": "/protected_tags/*",
    "type": "string",
    "doc": "A tag."
  },
  {
    "pointer": "/offset_in",
    "type": "bool",
    "default": false,
    "doc": "Only relevant for single body mode. Whether to create offset inside body"
  },
  {
    "pointer": "/offset_out",
    "type": "bool",
    "default": true,
    "doc": "Only relevant for single body mode. Whether to create offset outside body"
  },
  {
    "pointer": "/target_distance_rel",
    "type": "float",
    "default": 0.01,
    "doc": "Target offset distance relative to bounding box of mesh."
  },
  {
    "pointer": "/target_distance",
    "type": "float",
    "default": -1.0,
    "doc": "Target distance for offset. If < 0, the relative target distance is used to compute this one."
  },
  {
    "pointer": "/throw_on_nonconvergence",
    "type": "bool",
    "default": false,
    "doc": "If true, a run that finishes without meeting the convergence criteria raises an error instead of logging a warning. Default false: a non-converged offset is still a usable one, and the warnings already name which criterion failed. Integration tests set it true so a convergence regression fails the run rather than passing with a warning."
  },
  {
    "pointer": "/envelope_size_rel",
    "type": "float",
    "default": 0.001,
    "doc": "Half-width, relative to the bounding box diagonal, of the envelope containing every tag-region boundary during optimization. Any operation that would push a region boundary outside it is rejected. Ignored if envelope_size >= 0."
  },
  {
    "pointer": "/envelope_size",
    "type": "float",
    "default": -1.0,
    "doc": "Absolute half-width of the tag-region boundary envelope. If < 0, computed from envelope_size_rel."
  },
  {
    "pointer": "/offset_field",
    "type": "string",
    "default": "smooth",
    "options": [
      "smooth",
      "euclidean"
    ],
    "doc": "Which field defines the offset, and so what the front is placed on, what the criterion measures and what the sizing field refines by. 'smooth' (the default) is the offset geometric contact potential Phi, with the front on the level set Phi = c: C^2, analytic derivatives, and a level set that bulges outward at reentrant features, so it is a smoothed offset rather than the exact one. 'euclidean' is the exact distance to the input complex, which makes the residual exact but introduces a gradient discontinuity across the medial axis and a crease at every reentrant feature that no refinement resolves; offset_dhat_factor and the outside-the-support guard stop meaning anything, since it has no compact support."
  },
  {
    "pointer": "/offset_dhat_factor",
    "default": 2.0,
    "type": "float",
    "doc": "Support radius of the smooth offset potential, as a multiple of target_distance. Must be > 1. The offset level set lies strictly inside the support; a band vertex that leaves the support is a hard error."
  },
  {
    "pointer": "/front_conv_rel",
    "default": 0.025,
    "type": "float",
    "doc": "The convergence tolerance, read differently in each dimension. 2D: the accuracy knob, setting both the vertex bar (the remaining Newton step must be <= rel x target_distance) and the chord resolution threshold (a front edge is refinable while its sag over the level set exceeds rel x target_distance), so tighter means more turns and a finer front. Startup requires offset_envelope_rel <= front_conv_rel; see there. 3D: the convergence tolerance of the vertex test alone (Phase B's pass stop and the A/B loop's vertex half), where what it is a fraction of depends on front_conv_criterion; it does not set the resolution there, the edge test comparing the level set's sagitta with offset_envelope_rel x target_distance. Keep the 3D value small: a per-vertex, per-sweep tolerance cannot tell a slowly travelling front from a converged one."
  },
  {
    "pointer": "/front_conv_criterion",
    "type": "string",
    "default": "step_size_rel",
    "options": [
      "gradient_norm_rel",
      "step_size_rel",
      "decrement"
    ],
    "doc": "2D only. What 'converged' means for a front vertex, in Phase B's pass stop and in the loop's vertex test, measured against front_conv_rel. F is the vertex's Phase B objective, g its gradient, H its Gauss-Newton Hessian; under front_normal_projection both are taken along the vertex normal n. 'step_size_rel' (the default): the remaining 1-D Newton step, |n . g| / (n^T H n), against rel x target_distance. 'decrement': the Newton decrement, half of (n . g)^2 / (n^T H n), against rel x F(x). 'gradient_norm_rel': |n . g| against rel x a reference measured once on the band as constructed. Prefer the step or the decrement: the gradient is stiffness times remaining displacement, so the sliver between two meeting fronts reads far above the bar while sitting at its minimum."
  },
  {
    "pointer": "/offset_residual_samples",
    "default": 3,
    "type": "int",
    "doc": "Sampling density used to measure the offset's residual between its vertices. In 2D, k points in the interior of each band edge at i/(k+1), so k = 1 is the midpoint; in 3D, the interior lattice points of each offset-surface triangle at denominator k+2, so k = 1 is the centroid and the counts are 1, 3, 6, 10 for k = 1..4. 0 measures only at vertices, which is blind to a surface whose vertices sit on the level set while the elements between them cut across it."
  },
  {
    "pointer": "/sorted_marching",
    "type": "bool",
    "default": false,
    "doc": "Execute marching tets in decreasing order of edge length. Increases run time, may increase output mesh quality."
  },
  {
    "pointer": "/check_manifoldness",
    "type": "bool",
    "default": true,
    "doc": "After performing offset, check if offset region is manifold"
  },
  {
    "pointer": "/save_vtu",
    "type": "bool",
    "default": false,
    "doc": "Save .vtu of output mesh"
  },
  {
    "pointer": "/phi_grid_resolution",
    "type": "int",
    "default": 0,
    "doc": "Samples per side of the grid the smooth offset potential is written on, as <output>_phi.vtu, for the viewer. 0 disables it. In 2D the grid covers the whole domain; in 3D a volume is unaffordable, so it is the PLANE through the box centre normal to the box's shortest extent. Carries phi, phi_residual_length and euclidean_distance per vertex; warp by phi to see the field as a height surface."
  },
  {
    "pointer": "/DEBUG_output",
    "type": "bool",
    "default": false,
    "doc": "Write debug VTU frames of the evolving mesh. 2D: one frame after every operation pass and every smoothing pass, as sequential <output>_NNNNN.vtu files so ParaView loads them as one time series, plus <output>_frames.txt carrying one 'NNNNN<tab>r<turn><phase><pass>_<op>' line per frame, which visualize_offset.py reads for its labels. 3D: only the per-phase timeline, one frame after every Phase A and every Phase B (<output>_phase_{round}{A|B}.vtu); see DEBUG_output_per_pass for the full per-pass series."
  },
  {
    "pointer": "/DEBUG_output_per_pass",
    "type": "bool",
    "default": false,
    "doc": "Also write a debug VTU frame after every pass, not just the per-phase timeline: every Phase A local-operations pass and every Phase B smoothing pass, as <output>_debug_{N}.vtu on one shared counter so the two interleave in run order. Expect hundreds of frames and gigabytes on a real model. Observational only: the frames cannot change the run."
  },
  {
    "pointer": "/log_file",
    "type": "string",
    "default": "",
    "doc": "Logs are not just printed on the terminal but also saved in this file."
  },
  {
    "pointer": "/report",
    "type": "string",
    "default": "",
    "doc": "A JSON file that stores information about the result and the method execution, e.g., runtime."
  },
  {
    "pointer": "/preallocation_factor",
    "type": "float",
    "default": 6.0,
    "min": 1.0,
    "doc": "Mesh storage (connectivity and attributes) is preallocated to this factor times the live element count at init and at consolidation. Operations take fresh slots from that headroom and are retried once it is exhausted. Lower it for pure-decimation runs and raise it for aggressive refinement; the offset refines aggressively, so a run whose log carries [slots] warnings is dropping the very splits the criterion asked for."
  },
  {
    "pointer": "/num_threads",
    "type": "int",
    "default": 0,
    "doc": "Number of threads used for parallel execution (smoothing, edge collapse). 0 means single-threaded."
  },
  {
    "pointer": "/num_smoothing_passes",
    "type": "int",
    "default": 2,
    "doc": "Smoothing passes after the ONE combined split+collapse+swap round of the shared optimization loop when interleaved_smoothing is OFF. Not read otherwise: with interleaving on (the default) the count is interleaved_smoothing_passes, everywhere. TriWild's key and TriWild's default."
  },
  {
    "pointer": "/length_rel",
    "type": "float",
    "default": 0.05,
    "doc": "Target edge length relative to the bounding box diagonal. Used to bound which edges edge collapse is allowed to touch (only edges shorter than 4/5 of this length are collapsed). Ignored if length >= 0."
  },
  {
    "pointer": "/length",
    "type": "float",
    "default": -1.0,
    "doc": "Target edge length (absolute). If < 0, computed from length_rel."
  },
  {
    "pointer": "/stop_energy",
    "type": "float",
    "default": 100.0,
    "doc": "Target AMIPS quality for tets. Default 100, matching the tetwild component -- Phase A is TetWild and stops where TetWild stops. Do not lower it far: a target Phase A's operation set cannot deliver turns the split pass into an unbounded refiner. Edge collapse will not push an already-on-target region's quality back above this."
  },
  {
    "pointer": "/max_iterations",
    "type": "int",
    "default": 80,
    "doc": "Cap on the shared TriWild/TetWild loop wherever that loop runs: the pre-optimisation pass over the input, Phase A, and the finishing pass that runs with the front frozen. The loop exits as soon as max AMIPS is under stop_energy, so this is an upper bound. Where the loop is required to reach stop_energy, failing to within this many iterations is an error rather than a warning, because placing the offset on a mesh the improvement pass gave up on is not a result."
  },
  {
    "pointer": "/ab_smooth_max_passes",
    "type": "int",
    "default": -1,
    "doc": "Cap on Phase B's smoothing passes per round; negative (the default) means uncapped. Phase B runs until a pass completes with no offset vertex backtracked by its one-ring, which is its natural fixed point: the potential is a frozen field, so offset placements couple only through the ring constraint, and the interior AMIPS solves in the same passes are what open those rings. A no-progress guard stops the phase with a warning if neither the constrained count nor the placement gradient improves over 10 consecutive passes. Set a positive cap only as a diagnostic override. Read in both dimensions."
  },
  {
    "pointer": "/ab_smooth_tol",
    "type": "float",
    "default": 0.0001,
    "doc": "3D only. Phase B stops when the largest vertex displacement in a pass falls below this fraction of the target edge length l. Relative rather than absolute so the test means the same thing at any model scale."
  },
  {
    "pointer": "/vertex_grad_tol_rel",
    "type": "float",
    "default": 0.01,
    "doc": "Phase B's per-vertex tolerance for the interior (background AMIPS) Newton solves, in both dimensions: a visit stops when the local energy gradient falls below this fraction of its own value at the visit's start (polysolve's rel_grad_norm_tol). The 2D front placement does not read it -- that descent stops on the run's own bar instead -- so one parameter governs the local offset solves and the global criterion alike. Do not make the front placement entry-relative again: with no absolute floor it limit-cycles. 3D uses this key for both of its Phase B solves."
  },
  {
    "pointer": "/offset_envelope_rel",
    "type": "float",
    "default": 0.025,
    "doc": "Half-width of the corridor the operation passes must keep the front inside, as a fraction of target_distance, rebuilt after every smoothing pass. It is a leash, not an accuracy knob: front_conv_rel alone sets accuracy, and startup requires offset_envelope_rel <= front_conv_rel, because a wider leash lets the operations dent the front past the resolution threshold and mint new refinable edges every turn. Tighter over-constrains the passes and starts refusing operations that were not straddling the boundary at all. 3D applies it to offset_residual_tolerance() rather than to target_distance; that difference is deliberate."
  },
  {
    "pointer": "/interleaved_smoothing",
    "type": "bool",
    "default": true,
    "doc": "Run split, collapse and swap as three separate rounds with smoothing after each, instead of one combined round. TriWild's default."
  },
  {
    "pointer": "/interleaved_smoothing_passes",
    "type": "int",
    "default": 2,
    "doc": "Smoothing passes after each operation group (split, collapse, swap) when interleaved_smoothing is on -- in the shared TriWild/TetWild loop (the pre-optimisation pass, Phase A, the frozen-front finishing pass) and in the 2D single-phase loop. Default 2, against the shared engine's own 1: each smoothing pass is one placement sweep of the front, so at one pass per group the front contracts about half as fast for the same final result."
  },
  {
    "pointer": "/coarsen_pass",
    "type": "bool",
    "default": false,
    "doc": "Run the post-optimization coarsening pass, as TriWild and TetWild do. It trades elements for nothing but the guarantee that the result is still good, so topological_offset holds it to an absolute bar -- both AMIPS and the offset residual inside tolerance after each collapse -- rather than the non-degrading bar the main loop uses. Off by default: the pass can dominate total runtime, and being the last thing to run before Phase A checks convergence, anything it degrades is reported as a Phase A failure rather than a coarsening one."
  },
  {
    "pointer": "/coarsen_unbounded",
    "type": "bool",
    "default": true,
    "doc": "Coarsen as far as the quality guarantee allows, instead of stopping at the target edge length. The pass then answers how few elements can hold this max energy, which is more aggressive than it sounds because the answer ignores how big the elements become; measured over the registered models it roughly doubles the cell reduction while raising max energy on none of them. On by default. Turned off, it leaves alone any edge already at or past the collapse threshold, since collapsing it only makes its neighbours longer. The sizing field is deliberately not applied either way: honouring it here would leave the pass unable to undo refinement that turned out to be unnecessary."
  },
  {
    "pointer": "/coarsen_max_rounds",
    "type": "int",
    "default": 2,
    "doc": "Cap on the coarsen/smooth alternation; the pass also stops as soon as a round accepts no collapse. A round does not exist to finish what the previous one started: within a round the collapse pass already runs to a fixed point, so repeating the collapse alone finds nothing. What a round adds is the global smoothing in between, which moves that fixed point. Returns decay fast, and the default of two carries the large majority of the coarsening for a fraction of what the later rounds would cost."
  },
  {
    "pointer": "/coarsen_global_smoothing_passes",
    "type": "int",
    "default": 1,
    "doc": "Ordinary whole-mesh smoothing passes run between coarsening rounds. This is what makes a second round worth running at all -- see coarsen_max_rounds -- so setting it to 0 collapses the rounds to one."
  },
  {
    "pointer": "/coarsen_smooth_ring",
    "type": "int",
    "default": 1,
    "doc": "How far the local smoothing after each coarsening collapse reaches, in rings; the pass locks one ring wider than this. Deliberately 1 rather than TriWild's and TetWild's 2: a smaller ball perturbs less per collapse, so more collapses clear the offset's absolute coarsening bar."
  },
  {
    "pointer": "/coarsen_local_smoothing_passes",
    "type": "int",
    "default": 0,
    "doc": "Smoothing sweeps over the ring inside each candidate coarsening collapse, before its max energy is measured, so the collapse is judged on a relaxed neighbourhood rather than on the configuration it lands in. 0 by default: the ring is large enough that even two sweeps cost hundreds of nonlinear solves per candidate, and turning it off costs almost no cell reduction because the pass runs to a fixed point and finds another candidate instead. At 0 the only relaxation is the global smoothing between rounds, which has no per-operation accept test of its own -- see the forced quality veto in TetOptimizerMesh::coarsen_mesh()."
  },
  {
    "pointer": "/stuck_refine_stall_eps",
    "type": "float",
    "default": 0.1,
    "doc": "Fire the sizing refinement when an iteration's improvement is small next to the distance the metric still has to cover: (prev - cur) <= stall_eps * (cur - target). 0 means only when it does not improve at all."
  },
  {
    "pointer": "/stuck_refine_cooldown",
    "type": "int",
    "default": 1,
    "doc": "Iterations to skip after a sizing refinement before another may fire. Default 1, matching the tetwild component: the refined sizing needs one split/collapse round to act before the stall detector can fairly judge it. 0 allows one every iteration."
  },
  {
    "pointer": "/stuck_refine_num_worst",
    "type": "int",
    "default": 0,
    "doc": "How many worst faces seed the refinement. 0 means every face above the filter threshold, which is TriWild's default."
  },
  {
    "pointer": "/stuck_refine_rings",
    "type": "int",
    "default": 0,
    "doc": "Vertex rings grown around each seeded face before the sizing scalar is lowered."
  },
  {
    "pointer": "/stuck_refine_factor",
    "type": "float",
    "default": 0.5,
    "doc": "Multiplicative reduction of the per-vertex sizing scalar per refinement."
  },
  {
    "pointer": "/stuck_refine_min_scalar",
    "type": "float",
    "default": 0.001,
    "doc": "Floor on the per-vertex sizing scalar."
  },
  {
    "pointer": "/stuck_refine_gradation",
    "type": "float",
    "default": 2.0,
    "doc": "Neighbouring sizing scalars may differ by at most this factor. The smoothing only ever lowers a scalar, spreading refinement outward. <= 1 disables it."
  },
  {
    "pointer": "/stuck_refine_force_split",
    "type": "bool",
    "default": true,
    "doc": "On a stall, also split each seeded face's longest edge once, bypassing the length gate and without touching the sizing field."
  },
  {
    "pointer": "/front_normal_projection",
    "default": true,
    "type": "bool",
    "doc": "2D. Phase B places a front vertex by a one-dimensional solve along its field normal n = grad Phi/|grad Phi|: the same objective restricted to the line x0 + s n, with the same solver, line search and accept test, and the vertex test reads the step along that same n. Where a vertex sits along the front carries no information about the offset, and in the free 2-D solve that tangential motion is driven by AMIPS alone, which made fronts slide and fold where two of them meet. Spacing along the front is left to Phase A's smoother."
  },
  {
    "pointer": "/front_alignment_energy",
    "default": true,
    "type": "bool",
    "doc": "2D. Whether Phase B's front objective carries the alignment term: the sum over the vertex's live front edges of (1 - n_e . ghat(m_e))^2, with n_e the edge normal and ghat the field's unit gradient at the edge midpoint, weighted like the offset term. It is what acts on the edge normals under front_normal_projection, and what lets a pressed seam settle in a few turns rather than tens. On by default, with a known cost: under normal-only placement it can act only along the normal, so it biases a vertex a few percent of target_distance off its level set on curved fronts. Each edge's term is weighted by the gradient agreement at its endpoints, max(0, ghat_a . ghat_b), frozen per visit, so an edge spanning a concave corner's bisector gets no direction target instead of an impossible one."
  },
  {
    "pointer": "/sizing_collapse_min",
    "default": false,
    "type": "bool",
    "doc": "2D. What the surviving vertex of a collapse keeps as its sizing scalar. false (the default): its own scalar, restored after the base collapse. true: the smaller of the two, which is the shared engine's rule. The min rule carries substantially more vertices and lengthens the placement loop where the front travels, because the offset's sizing follows a moving front and has to relax behind it, which a scalar that only ever falls cannot do."
  },
  {
    "pointer": "/max_rounds",
    "default": 40,
    "type": "int",
    "doc": "The outer loop's budget, in both dimensions. 2D: the number of turns, one turn being split, collapse and swap with smoothing after each, followed by the front test. 3D: the number of A/B rounds, one round being a full Phase A followed by a full Phase B. An upper bound rather than a schedule -- exhausting it is a warning and not an error, because a front that needs more turns is a legitimate outcome to inspect."
  },
  {
    "pointer": "/pre_optimize_sizing_from_edges",
    "default": false,
    "type": "bool",
    "doc": "Which sizing field pre_optimize_input runs against. false (the default): seed target_distance / l on the input-complex boundary and grade outward, so the complex is resolved at the offset's own scale. true: seed every vertex from the mean of its own incident edge lengths instead, so target_distance never enters the field; that improves element quality without refining toward delta, so the constructed offset lands wherever the input mesh's own scale puts it. Only read when pre_optimize_input is true."
  },
  {
    "pointer": "/pre_optimize_input",
    "default": true,
    "type": "bool",
    "doc": "2D only. Run TriWild over the input mesh before the offset is constructed -- before the simplicial embedding and the marching -- with the same Phase A parameters and held only by the per-tag region envelopes, which exist by then. The sizing field is seeded on the input-complex boundary alone and graded outward, so the complex is resolved at the offset's own scale and the marching band comes out one delta-scale cell thick. That matters because the marching places the offset on the background triangulation's own cell boundaries: a coarse cell touching the complex inflates dhat, and a dhat several times target_distance merges the level sets of nearby features. On by default."
  },
  {
    "pointer": "/min_edge_length",
    "type": "float",
    "default": -1.0,
    "doc": "l_min: the shortest edge the sizing field may ask for, in absolute units. If < 0, derived as min_edge_length_rel * target_distance -- tied to the offset distance rather than the bounding box, because the offset is what has to be resolved. This is a floor on REFINEMENT, so raising it makes the offset coarser (paper Fig. 18)."
  },
  {
    "pointer": "/min_edge_length_rel",
    "type": "float",
    "default": -1.0,
    "doc": "l_min as a multiple of target_distance, used when min_edge_length is negative. Negative (the default) derives it from the Phase A offset envelope epsilon, which is just offset_envelope_rel: TetWild's own floor (cap the sizing field below by the envelope eps, Sec 3.2) stated in the offset's units, since the front is only held to within eps and edges shorter than eps cannot buy fidelity. A pure anti-runaway rail, far below the chord length any tolerance actually needs."
  },
  {
    "pointer": "/min_sizing_scalar",
    "type": "float",
    "default": 0.01,
    "doc": "Lower bound for the per-vertex sizing field (see max_sizing_scalar). Refinement stops once a vertex's sizing scalar reaches this fraction of the base target length."
  },
  {
    "pointer": "/max_sizing_scalar",
    "type": "float",
    "default": 1.0,
    "doc": "Upper bound for the per-vertex sizing field, as a multiplier on the target edge length (splitting_l2/collapsing_l2). 1.0 means never coarser than the base target length."
  },
  {
    "pointer": "/sizing_gradation",
    "type": "float",
    "default": 2.0,
    "doc": "Gradation cap for the sizing field: neighbouring vertices' sizing scalars may differ by at most this factor. After each refinement pass the refined vertices' lower sizing scalar is propagated outward -- monotone, only ever lowering a neighbour's scalar -- so the mesh does not jump straight from fine to coarse. <= 1 disables gradation."
  },
  {
    "pointer": "/split_high_valence_threshold",
    "type": "int",
    "default": 0,
    "doc": "Incident-tet count above which a vertex in a split edge's link accepts only one valence-increasing split per pass, or 0 to disable. Spreads refinement instead of letting it pile onto one vertex (only used if optimize is true)."
  },
  {
    "pointer": "/project_line_search_steps",
    "type": "int",
    "default": 12,
    "doc": "Bisections tried by the projected line search before it gives up on a vertex: the step along the Newton direction is halved this many times, each candidate projected onto the input, and the first that does not invert and lowers the worst incident element is taken."
  },
  {
    "pointer": "/project_line_search_nested_steps",
    "type": "int",
    "default": 0,
    "doc": "After the projected line search gives up, revisit each candidate and bisect between the interpolated point and its projection this many times, taking the longest step toward the input that still does not invert and still lowers the worst element. Lets a vertex whose one-ring cannot tolerate a full projection still travel toward the input, instead of being refused every pass from the same place. 0 disables the pass."
  },
  {
    "pointer": "/smooth_quality_veto",
    "type": "bool",
    "default": true,
    "doc": "Whether smoothing refuses a move that raises the worst incident element's quality. True is TriWild's, SimWild's and TetWild's behaviour and the default here; 3D ignores this key and forces true. Phase B places the front with its own solver and Phase A moves it by AMIPS alone inside the offset envelope, so Phase A is TriWild and takes TriWild's veto. The key stays settable for experiments."
  },
  {
    "pointer": "/smoothing_mode",
    "type": "string",
    "default": "projected",
    "options": [
      "projected",
      "exact"
    ],
    "doc": "How smoothing places a surface vertex. 'projected': smooth with AMIPS alone as if interior, then walk back toward the start projecting each candidate onto the input; accept the first projected candidate that does not invert and strictly lowers the worst incident element, else do not move. Lands exactly on the input; no weights. 'exact': minimize w_amips * AMIPS + (1-w_amips) * (d/eps)^2 with the true region-wise Hessian of the distance to the piecewise-linear input; sliding is free where the input is flat, held at corners, constrained along 3D edges and curve segments. Rests a w_amips-proportional distance off the input."
  },
  {
    "pointer": "/w_amips",
    "type": "float",
    "default": 0.0001,
    "doc": "Relative weight of the AMIPS quality term against the envelope term during smoothing of non-offset-surface vertices. The envelope weight is derived as 1 - w_amips (only used if optimize is true)."
  },
  {
    "pointer": "/perform_sanity_checks",
    "type": "bool",
    "default": false,
    "doc": "Check after every operation pass that no tet is inverted and no tracked-surface triangle left its envelope. Slow; for debugging (only used if optimize is true)."
  },
  {
    "pointer": "/deform_others",
    "default": true,
    "type": "bool",
    "doc": "2D. Whether the other objects in the scene -- tagged regions with no input-complex face and no domain-wall contact -- deform when the optimization pushes them, instead of being held by their per-tag envelopes. true (the default): their envelopes are dropped at the top of optimize_offset(), every face of theirs is stamped with its rest shape, and smoothing carries a rest-shape AMIPS term for those faces weighted like the smoother's own. A face keeps that rest shape until an operation touches it; an accepted split, collapse or swap re-stamps the faces it changed, which is what makes the scheme indifferent to remeshing. protected_tags opts a region out, and the offset's own complex, the domain wall and the curve group are always held."
  }
]