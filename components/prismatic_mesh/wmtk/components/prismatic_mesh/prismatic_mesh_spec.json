[
    {
        "pointer": "/",
        "type": "object",
        "required": [
            "application",
            "input",
            "output"
        ],
        "optional": [
            "input_dir",
            "thicknessratio",
            "iterations",
            "min_tet_volume",
            "smoothing_max_backtracks"
        ]
    },
    {
        "pointer": "/application",
        "type": "string",
        "options": [
            "prismatic_mesh"
        ]
    },
    {
        "pointer": "/input",
        "type": "string",
        "doc": "Construction VTU with labels, vid, corr_input_vid, tag_0 and offset_tag."
    },
    {
        "pointer": "/output",
        "type": "string",
        "doc": "Output VTU filename, relative to the JSON directory. A missing extension becomes .vtu."
    },
    {
        "pointer": "/input_dir",
        "type": "string",
        "default": "",
        "doc": "Base directory for input; set by wmtk_app to the JSON directory."
    },
    {
        "pointer": "/thicknessratio",
        "type": "float",
        "default": 0.1,
        "doc": "Positive target thickness relative to the mean length of unique input surface edges."
    },
    {
        "pointer": "/iterations",
        "type": "int",
        "default": 5,
        "doc": "Number of collapse/smoothing iterations; 0 computes targets without optimization."
    },
    {
        "pointer": "/min_tet_volume",
        "type": "float",
        "default": 1e-12,
        "doc": "Nonnegative strict lower bound on signed tetrahedron volume, in coordinate units cubed."
    },
    {
        "pointer": "/smoothing_max_backtracks",
        "type": "int",
        "default": 40,
        "doc": "Maximum smoothing step halvings after the full target step; between 0 and 60."
    }
]
